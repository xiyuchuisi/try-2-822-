class CamvidDataset(Dataset):
    def __init__(self, file_path=[], crop_size=None):
        if len(file_path) != 2
            raise ValueError("同时需要")
        self.img_path = file_path[0]
        self.labels = 
